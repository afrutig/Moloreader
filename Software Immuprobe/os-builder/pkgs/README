A package consists mainly of a Makefile that must provide following targets:

prepare: This is called while creating the disk image. Downloading files, copying, ...

install: Runs on the RaspberryPI.

clean: Remove all files generated by prepare.
